version: 2

models:
  - name: stg_password
    description: "Staging model for password table from public schema"
    columns:
      - name: rowguid
        description: "rowguid (character varying, nullable)"
      - name: modifieddate
        description: "modifieddate (timestamp without time zone, nullable)"
      - name: passwordhash
        description: "passwordhash (character varying, nullable)"
      - name: passwordsalt
        description: "passwordsalt (character varying, nullable)"
      - name: businessentityid
        description: "businessentityid (bigint, nullable)"
