version: 2

models:
  - name: stg_productcosthistory
    description: "Staging model for productcosthistory table from public schema"
    columns:
      - name: enddate
        description: "enddate (timestamp without time zone, nullable)"
      - name: productid
        description: "productid (bigint, nullable)"
      - name: startdate
        description: "startdate (timestamp without time zone, nullable)"
      - name: modifieddate
        description: "modifieddate (timestamp without time zone, nullable)"
      - name: standardcost
        description: "standardcost (numeric, nullable)"
